pipeline {
    agent { label 'ubuntu-10.6.5.21' }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
                echo 'install success!'
            }

        }
    }
}
